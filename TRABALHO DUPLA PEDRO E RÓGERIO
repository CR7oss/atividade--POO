CÓDIGO PEDRO

# Criar a matriz 3x3
matriz = [[0, 0, 0], [0, 0, 0], [0, 0, 0]]

# Ler os valores do teclado
for l in range(3):
    for c in range(3):
        matriz[l][c] = int(input(f'Digite um valor para [{l}, {c}]: '))

print('-' * 30)
print('Matriz:')
# Mostrar a matriz formatada
for l in range(3):
    for c in range(3):
        print(f'[{matriz[l][c]:^5}]', end='')
    print()  # quebra de linha

print('-' * 30)

# A) Soma dos valores pares
soma_pares = 0
for l in range(3):
    for c in range(3):
        if matriz[l][c] % 2 == 0:
            soma_pares += matriz[l][c]

# B) Soma da terceira coluna
soma_coluna3 = matriz[0][2] + matriz[1][2] + matriz[2][2]

# C) Maior valor da segunda linha
maior_linha2 = max(matriz[1])

# Mostrar os resultados
print(f'A) Soma dos valores pares: {soma_pares}')
print(f'B) Soma da terceira coluna: {soma_coluna3}')
print(f'C) Maior valor da segunda linha: {maior_linha2}')

O código do Rogério está correto e bem estruturado. A leitura e exibição da matriz estão claras e funcionam conforme o solicitado.
Sugestão: poderia usar max(matriz[1]) para simplificar a busca do maior valor da segunda linha, e usar sum(matriz[i][2] for i in range(3)) para simplificar a soma da terceira coluna.

CÓDIGO ROGÉRIO

# Criar a matriz 3x3
matriz = []

# Ler os valores do teclado
for i in range(3):
    linha = []
    for j in range(3):
        valor = int(input(f'Digite o valor para [{i}, {j}]: '))
        linha.append(valor)
    matriz.append(linha)

print('-' * 30)
print('Matriz:')
# Mostrar a matriz na tela
for i in range(3):
    print(f'{matriz[i][0]:^5}{matriz[i][1]:^5}{matriz[i][2]:^5}')

print('-' * 30)

# A) Soma dos números pares
soma_pares = 0
for i in range(3):
    for j in range(3):
        if matriz[i][j] % 2 == 0:
            soma_pares += matriz[i][j]

# B) Soma da 3ª coluna
soma_coluna3 = 0
for i in range(3):
    soma_coluna3 += matriz[i][2]

# C) Maior número da 2ª linha
maior_linha2 = matriz[1][0]
for j in range(3):
    if matriz[1][j] > maior_linha2:
        maior_linha2 = matriz[1][j]

# Mostrar resultados
print(f'A) Soma dos pares: {soma_pares}')
print(f'B) Soma da 3ª coluna: {soma_coluna3}')
print(f'C) Maior valor da 2ª linha: {maior_linha2}')

O código do Pedro está correto e cumpre todos os requisitos da atividade.
Sugestão: simplificar algumas partes do código para deixá-lo mais direto, como usar sum() para somar a terceira coluna e max() para o maior valor da segunda linha.
